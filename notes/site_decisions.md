# Lana Svitla — портфолио v2: принятые решения

Постоянный контекст нового сайта. Здесь только решения, которые нельзя вывести из
кода: договорённости, осознанные отклонения от эталона и правила на будущее.

- **Проект:** `/Users/lanasvitla/Agentic_os/Agentic_os26/work/lanes/work/active/project/lana_portfolio_next/site`
- **Статус:** приоритетная активная версия портфолио с 2026-08-21; V1 поставлена на паузу.
- **Git:** https://github.com/lanasvitla/ls-folio, ветка `main`, commit `0fb4470`.
- **GitHub Pages:** ожидаемый адрес `https://lanasvitla.github.io/ls-folio/`; публикация ждёт включения Pages из `main` / `/ (root)`.
- **Эталон стиля:** https://gocetinkaya.com
- **Старый сайт:** `ls-portfolio` — V1, пока остаётся live; позже планируется отдельный подтверждённый этап редиректа на V2.
- **Рабочий скилл:** `qwick_block` (`~/.codex/skills/qwick_block/SKILL.md`)

## Архитектура

Статический сайт, общие блоки собираются из партиалов:

```
partials/site-header.html   компонент
partials/site-footer.html   компонент
partials/social-links.html  вложенный компонент, подключается через {{> social-links}}
tools/pages.json            манифест страниц и значений
tools/render_partials.py    сборщик
```

Сборка: `python3 tools/render_partials.py` (проверка без записи — `--check`).
Правки общих блоков делать **только в партиалах**, не в собранных страницах.

Любой блок, встречающийся больше чем на одной странице, обязан быть компонентом.

## Анимация появления контента

Единое правило для **всех страниц** сайта: блоки выезжают **справа налево** с
затуханием, каскадом — каждый следующий на 110 мс позже предыдущего.

- Реализация: класс `.reveal` + `IntersectionObserver` в `assets/js/main.js`.
- `--i` — позиция элемента **внутри пачки, появляющейся одновременно**, а не в
  документе. Иначе одиночный блок, доскроленный до экрана, ждал бы чужую задержку.
- Длительность 0.8s, кривая `--ease`.
- При системной настройке «уменьшить движение» сдвиг и задержки отключаются.
- Новый блок анимируется автоматически — достаточно повесить на него `reveal`.

## Типографика (обязательна для всего русского текста)

- Короткие предлоги/союзы не висят в конце строки — склеивать `&nbsp;`.
- Буква «ё» не используется, пишем «е».
- Тире среднее `–`, не длинное `—`.

Правится **скриптом, не руками**: `python3 tools/typography.py`
(проверка без записи — `--check`). Скрипт трогает только текст между тегами,
не заходит в атрибуты, `<script>`, `<style>` и `<svg>`, идемпотентен.

Порядок при правке общих блоков: сначала `typography.py` (включая партиалы),
затем `render_partials.py`.

## Осознанные отклонения от эталона

| Что | В эталоне | У нас | Почему |
|---|---|---|---|
| Ширина лида на контактах | 400px | 620px | русский текст длиннее английского примерно на треть |
| Поля контента | 80px везде | 80px только на контактах, на остальных `--page-x` | контент прочих страниц выровнен по своей сетке |
| Хедер на мобильном | — | отступы 14px со всех сторон | решение автора; хедер чуть шире сетки контента |
| Контур кнопки в футере | появляется при ховере | так же | совпадает |
| Открывание футера при скролле | нет (футер статичный) | есть | осознанно добавлено по просьбе автора |

## Особенности, которые легко сломать

- **Футер закреплён** (`position: fixed`) и открывается из-под контента. Поверх него
  лежит непрозрачный слой `.page`, включающий хедер, — без него футер просвечивает
  сквозь прозрачный хедер вверху страницы.
- Если футер выше экрана (короткие телефоны), закрепление автоматически отключается
  классом `no-footer-reveal` — порог считается в `main.js`.
- На странице контактов футера нет намеренно (как в эталоне), только копирайт.
- Плашка хедера имеет свой еле заметный фон **всегда**, а не только при скролле.

## Ждёт решения автора

- Дальнейшая структура, тексты и визуальная подача главной страницы и Works.
- Полное содержание внутренних кейсов: текущие страницы ещё не раскрывают кейсы до конца.
- Включить GitHub Pages для `lanasvitla/ls-folio`: `Deploy from a branch`, `main`, `/ (root)`.
- После проверки V2 отдельно согласовать охват редиректа V1 → V2.

## Выполнено

- Через новый шаблон и визуальный стиль объединена подача бренд- и продуктовой экспертизы.
- Проработаны страницы «Обо мне» и «Контакты».
- Для двух направлений подключены отдельные CV: `lana-svitla-cv-product-designer.pdf` и `lana-svitla-cv-brand-designer.pdf`.
- Репозиторий `lanasvitla/ls-folio` создан, первый commit загружен в `main`.

## Открытый бриф

- [about_page_brief.md](about_page_brief.md) сохранён как исходный бриф; актуальное содержание уже внесено в `about.html`, включая образование и курсы.
- Имя на сайте везде пишется в английском варианте: **Lana Svitla**.
